-- supabase/seed.sql
-- Alias for migration seeds — run this for fresh dev environment

-- Destinations are seeded via migration 001_initial_schema.sql
-- Re-run that migration file to repopulate destinations.

SELECT 'Seed complete. Destinations seeded via migration 001_initial_schema.sql' AS status;
